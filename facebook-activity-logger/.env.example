# Optional Meta Graph API token. Keep this private and never commit your real token.
FACEBOOK_ACCESS_TOKEN=

# SQLite database file path (relative to project root if not absolute)
DATABASE_PATH=./facebook_activity.db
